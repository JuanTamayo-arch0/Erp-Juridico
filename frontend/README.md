# Frontend — Local development

Prerequisites:
- Node.js (16+)
- npm

Install dependencies and run dev server:

```bash
cd frontend
npm install
npm run dev
```

The Vite dev server will serve at http://localhost:5173 by default.
